import 'package:flutter/material.dart';

class Autheniticate extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Autheniticate"),
    );
  }
}